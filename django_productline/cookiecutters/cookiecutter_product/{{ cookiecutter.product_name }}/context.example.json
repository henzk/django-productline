{
    "SECRET_KEY": "{{ cookiecutter.secret_key }}",
    "SITE_ID": 1,
    "DATA_DIR": "<path_to_webapps>/<container_name>/products/{{ cookiecutter.product_name }}/__data__",
    "DATABASES": {
        => Remove one of the following configurations
        "default": {
            "ENGINE": "django.db.backends.sqlite3",
            "NAME": "<path_to_webapps>/<container_name>/products/{{ cookiecutter.product_name }}/__data__/db.sqlite"
        }
        "default": {
            "ENGINE": "django.db.backends.postgresql_psycopg2", 
            "HOST": "localhost", 
            "PASSWORD": "mypassword", 
            "NAME": "mydatabse", 
            "USER": "mydatabaseuser"
        }
    }
}
